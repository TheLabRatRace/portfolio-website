---
name: Feature
about: Something the site should do that it does not do yet.
title: ''
labels: feature
---

### The problem

<!-- Describe what is hard or impossible today, from the reader's side of the
     screen. Skip the solution for a moment — the problem is the part that
     survives if the implementation changes. -->

### Proposed change

### Scope

- [ ] template / CSS only
- [ ] route or query change
- [ ] schema change (`migrate.sql` for a fresh database, `schema_admin_search.sql`
      for anything that must also reach a live one)
- [ ] new tooling in `tools/`
- [ ] build, image, or pipeline

### Does this fragment anything?

<!-- Required. Name the existing component, helper, or CSS block this extends,
     or say plainly that nothing like it exists yet. A second way of doing an
     existing thing needs a reason. -->

### How we would know it worked

<!-- A measurement, a test, or a specific thing to look at. "It looks better"
     is not one; "the gallery card fetches the 360w file at 375px" is. -->
